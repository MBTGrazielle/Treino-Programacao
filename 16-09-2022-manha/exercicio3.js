//velocidade=distancia/tempo
// velocidade?
// distancia=100
// tempo=100

function calcularVelocidadeMedia(distancia, tempo) {
    let velocidade = (distancia / tempo) * 3.6;
    console.log(velocidade);
}

calcularVelocidadeMedia(100, 100);
